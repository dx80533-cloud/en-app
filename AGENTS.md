# 英文單字庫複習 App

## 產品概述
面向台灣國中生的英文單字學習應用，支援全民英檢（GEPT）、多益（TOEIC）、雅思（IELTS）三大考試字庫，共 8160 個單字。以問答互動方式學習，結合遊戲化機制激勵持續學習。

## 技術架構
- 前端：React 19 + TypeScript + Tailwind CSS + shadcn/ui
- 後端：NestJS 10 + Drizzle ORM + PostgreSQL
- 數據庫表：vocab_words（單字庫）、vocab_learning_progress（學習進度）、vocab_quiz_records（測驗記錄）、vocab_user_stats（使用者統計）

## 頁面結構
- `/` — 首頁（學習儀表板：進度卡片、連續天數、今日任務、快速開始）
- `/learn` — 學習頁（多題型練習：看英文選中文、看中文選英文、聽音選義、拼字、填空）
- `/quiz` — 測驗頁（模式選擇 + 測驗進行 + 成績結果）
- `/vocabulary` — 字庫瀏覽頁（字庫選擇、篩選、搜尋、收藏）
- `/collection` — 收藏單字頁
- `/about` — 關於頁（資料來源說明）

## 設計規範

### 風格定位
活潑、明亮、遊戲化，符合國中生審美。色彩繽紛但不混亂，圓角卡片，柔和動畫回饋。

### 色彩系統
- 主色：藍紫色系 `indigo-500`（代表智慧與學習）
- 輔助色：珊瑚橘 `orange-400`（強調 CTA）、薄荷綠 `emerald-500`（答對/成功）、玫瑰紅 `rose-500`（答錯/警告）
- 背景：淺灰 `slate-50`，卡片純白
- 文字：主文字 `slate-800`，次文字 `slate-500`

### 間距基線
- 頁面左右內邊距：移動端 `px-4`，桌面端 `px-6`
- 卡片內邊距：`p-5`（標準）、`p-6`（大卡）
- 元素間距：`gap-3`（小）、`gap-4`（標準）、`gap-6`（大區塊）
- 區塊上下距：`py-6`（頁面）、`space-y-4`（卡片堆疊）

### 圓角與陰影
- 卡片圓角：`rounded-2xl`
- 按鈕圓角：`rounded-xl`
- 輸入框圓角：`rounded-lg`
- 卡片陰影：`shadow-sm`（預設）、`shadow-md hover:shadow-lg`（互動元素）

### 排版
- 標題：`text-2xl font-bold`（頁標題）、`text-lg font-semibold`（卡片標題）
- 內文：`text-base`（標準）、`text-sm`（輔助）
- 數字/統計：`text-3xl font-bold`（大數字）

### 動畫
- 按鈕/卡片 hover：輕微上浮 + 陰影加深，transition 150ms
- 答對動畫：綠色閃爍 + 微彈跳
- 答錯動畫：紅色輕微抖動
- 頁面切換：淡入，150ms

## 核心功能清單
1. 多題型問答學習（5 種題型）
2. 單字發音播放（Web Speech API）
3. 例句展示（自動補齊缺例句的單字）
4. 學習進度追蹤（已學、待複習、連續天數、答對率）
5. 遊戲化機制（XP、等級、徽章、連勝）
6. 字庫選擇與篩選（考試/等級/詞頻/字母）
7. 收藏單字
8. 測驗模式
9. 響應式設計（手機/電腦）
